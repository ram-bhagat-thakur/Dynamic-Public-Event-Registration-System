# MongoDB connection string
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/event-db

# JWT secret key for admin authentication
JWT_SECRET=your_jwt_secret_key

# API key for sending email confirmations (e.g., google app)
EMAIL_USER=email_id
EMAIL_PASS=email_app_pasword

VITE_API_BASE_URL=http://localhost:5000




# ========================
# 🌐 Backend Configuration
# ========================

# MongoDB connection string (replace <username> and <password> with actual credentials)
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/event-db

# JWT secret key for admin authentication
JWT_SECRET=your_jwt_secret_key

# Email credentials for sending confirmation emails (e.g., Gmail SMTP or app password)
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_app_password


# ========================
# ⚙️ Frontend Configuration
# ========================

# Base URL for API requests (used by axiosInstance.js)
VITE_API_BASE_URL=http://localhost:5000